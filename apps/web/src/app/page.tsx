/**
 * Placeholder index. This workspace is an API surface first — the routes under
 * `src/app/api/` are the product. A real UI can replace this page later.
 */
export default function Home() {
	return (
		<main>
			<h1>AZOR API</h1>
			<p>
				Browser-facing API for the AZOR platform. See <code>/api/version</code> and the
				routes under <code>/api/</code>.
			</p>
		</main>
	)
}
